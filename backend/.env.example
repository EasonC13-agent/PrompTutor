# Server
PORT=3456
NODE_ENV=production

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/promptutor

# Google OAuth (for student authentication)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Admin emails (comma-separated, can view all data)
ADMIN_EMAILS=researcher@example.com

# Anthropic API key (for answer-seeking detection)
ANTHROPIC_API_KEY=sk-ant-xxx

# CORS (optional, defaults to allow all origins)
CORS_ORIGIN=*
